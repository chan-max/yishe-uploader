# Ozon 平台说明

当前目录只完成了平台占位、参数结构与文档示例，尚未进入真实 DOM 调试阶段。

## 后续补充顺序

1. 搜索页商品卡片选择器
2. 商品详情主信息区
3. 店铺商品列表

## 维护约定

- 所有选择器放在 `selectors.js`
- 平台说明与可用性在 `index.js` 的 `capability` 中维护
- 真实调试后再把场景状态从 `unsupported` 调整为 `heuristic` / `available`
